namespace SuperheroesDb_Project.Models
{
    public class CustomerSpender
    { // We only include Total because that's the only thing needed in the assignment (Same as certain values from Customer)
        public int Total { get; set; }
    }
}
